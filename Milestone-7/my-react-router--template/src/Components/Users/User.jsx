import React from 'react';
import { useLoaderData } from 'react-router';

const User = () => {
    const users = useLoaderData(loader)
    return (
        <div>
            
        </div>
    );
};

export default User;